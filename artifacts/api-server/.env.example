# Supabase
SUPABASE_URL=your_supabase_url
SUPABASE_SERVICE_KEY=your_supabase_service_key
SUPABASE_ANON_KEY=your_supabase_anon_key

# Anthropic (Claude) — powers the AI chat assistant
ANTHROPIC_API_KEY=your_anthropic_api_key

# WhatsApp via Gupshup
# Optional app-level fallback. Per-business keys are saved in the DB via the
# Integrations page and take precedence over these.
GUPSHUP_API_KEY=your_gupshup_api_key
GUPSHUP_SOURCE_NUMBER=917xxxxxxxxx
GUPSHUP_APP_NAME=MarketMyBusiness

# Email via Brevo
# BREVO_SENDER_EMAIL must be a sender address verified in your Brevo account,
# otherwise Brevo rejects the send.
BREVO_API_KEY=xkeysib-xxxxxxxxxx
BREVO_SENDER_EMAIL=marketing@yourdomain.com
